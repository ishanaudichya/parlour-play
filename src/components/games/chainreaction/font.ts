/* The reactor's headline face. Chakra Petch — angular, technical, a little
   sharp-edged — unlike anything else on the platform (Bungee is the arcade
   cabinet, Baloo is UNO, the serifs belong to the shell and the courts). */

import { Chakra_Petch } from "next/font/google";

export const chakra = Chakra_Petch({ weight: ["600", "700"], subsets: ["latin"], display: "swap" });
