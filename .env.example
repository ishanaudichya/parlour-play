# Upstash Redis (required in production; local dev falls back to in-memory rooms).
# Easiest setup: Vercel dashboard → Storage / Marketplace → Upstash Redis → connect
# to the project. That injects these automatically (KV_REST_API_* names also work).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
